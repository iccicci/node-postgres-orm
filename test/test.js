/* jshint mocha: true */
'use strict';

var assert = require('assert');
var pgo = require('../lib/pgo');

describe('pgo', function() {
	/*
	describe('async', function() {
		before(function(done) {
			var test = this;
			
			pgo('', function(err, res) {
				test.err = err;
				test.res = res;
				
				done();
			});
		});

		describe('body', function() {
			
		});
		
		describe('database', function() {
			before(function(done) {
				done();
			});
		});
		
		it('err should be null', function() {
			assert.ifError(this.err);
		});
		it('res should be {}', function() {
			assert.deepEqual({}, this.res);
		});
		
	});
	
	it('should export hello world', function(done) {
		pgo('', function(err, res) {
			assert.ifError(err);
			
			assert.deepEqual({}, res);
			
			done();
		});
	});
	*/
});
 